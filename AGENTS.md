# AGENTS.md

Agent guidance for this repository lives in **[CLAUDE.md](./CLAUDE.md)** — the single source of truth
for the project overview, layout, commands, where to change behavior, and code style.

Read that file. Everything in it applies regardless of which agent or tool you are (Codex, Cursor,
Claude Code, etc.). This pointer exists so the guidance never drifts between two copies.
